<!DOCTYPE html>

<title>New Extra</title>

{% block body %}
    <h1>Create new Extra</h1>

    {{ include('extra/_form.html.twig') }}

    <a href="{{ path('extra_index') }}">back to list</a>
{% endblock %}
