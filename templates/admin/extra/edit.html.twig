<!DOCTYPE html>

<title>Edit Extra</title>

{% block body %}
    <h1>Edit Extra</h1>

    {{ include('extra/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('extra_index') }}">back to list</a>

    {{ include('extra/_delete_form.html.twig') }}
{% endblock %}
