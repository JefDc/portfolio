{% block body %}
    <section class="milestones-section spad" id="competence">
        <div class="container-fluid">
            <div class="row">
                <div class="comp offset-1">
                    {% for extra in extras %}
                        <h2 class="text-white competence">{{ extra.titleSkill }}</h2>
                    {% endfor %}
                </div>
            </div>
            <div class="container">
            <div class="row">
                {% for skill in skills %}
                    <div class="col-lg-3 col-md-6 fact-box mt-5">
                        <div class="text-center">
                            <img class="" src="/home/img/skill/{{ skill.img }}">
                            <p class="text-white m-4">{{ skill.title }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            </div>
        </div>
    </section>
{% endblock %}